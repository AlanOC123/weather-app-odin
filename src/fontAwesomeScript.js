const head = document.head;

const faScript = document.createElement('link');

faScript.href = 'https://use.fontawesome.com/releases/v6.7.0/css/all.css';
faScript.rel = 'stylesheet';

head.append(faScript);